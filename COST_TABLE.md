# SPI LEARNING — Honest Cost Table — 100% Free Except Optional Anthropic

**Date:** 2026-09-19
**Honest statement:** The ONLY paid piece is optional Anthropic API for main notes analysis — that's expected and fine. Everything else is genuinely free, no hidden paid dependency. If you don't set ANTHROPIC_API_KEY, app uses 100% free Puter.js.

| Feature | Tool / Service Used | Free or Paid? | Real Cost / Free Tier Limit | What Happens at Free Limit? | Honest Notes |
|---------|---------------------|---------------|------------------------------|-----------------------------|--------------|
| **Transcript extraction** | YouTube captions API (youtube.com/api/timedtext) + oembed (youtube.com/oembed) + Invidious fallback (invidious.snopyta.org) | **FREE** — no API key, no billing, open | Unlimited, no key required. YouTube oembed is public, timedtext is public captions endpoint, Invidious is open-source. | If YouTube blocks request or video has no captions: returns 404 with clear message "No transcript/captions available — we will NOT fake content". Falls back to Invidious instances. If all fail: honest error, NOT fake notes. | Reads real captions/transcript with timestamps — does NOT literally watch video. Honest about it in UI. Chunking preserved for long videos 3h+ (splits into 100-segment chunks). |
| **Notes analysis AI (main)** | **Primary (free):** Puter.js free GPT-4o-mini via https://js.puter.com/v2/ — client-side, no key, unlimited | **FREE** — genuinely free unlimited, no billing | Free unlimited, no rate limit documented, client-side. Loads script from js.puter.com, calls puter.ai.chat. | Always works, even offline has local smart fallback. If Puter.js fails: local template specific to topic (react/js/python etc) but marked as fallback. | This is the main notes generation — uses real transcript as input, organizes into Full Transcript [00:00], Important Words "exact phrase" [02:15], Key Moments, Quick Captures. Honest: reads transcript, not watches video. |
| **Notes analysis AI (optional paid)** | Anthropic Claude claude-3-5-sonnet via api.anthropic.com (if ANTHROPIC_API_KEY set) | **PAID** — $3 per 1M input tokens, $15 per 1M output tokens | Example: 10 min video ~1500 words ~2000 tokens → ~$0.006 input + $0.015 output = ~$0.02 per video. 1 hour video ~$0.10. | If no ANTHROPIC_API_KEY env: automatically uses free Puter.js — no break, graceful degradation. If key set but quota exceeded: falls back to Puter.js free with clear log. | NOT required — free works fine. Only if you want Claude quality. We state honestly cost, don't call it free if it isn't. |
| **Chat / Support AI Assistant widget** | **Primary:** Gemini free tier gemini-1.5-flash via generativelanguage.googleapis.com — 15 RPM, 1M TPM, genuinely free, no billing (requires GEMINI_API_KEY) <br> **Fallback 1:** Groq free tier llama-3.1-8b-instant via api.groq.com — 14,400 requests/day, genuinely free (requires GROQ_API_KEY) <br> **Fallback 2:** Puter.js free unlimited gpt-4o-mini via js.puter.com/v2/ — no key, client-side <br> **Fallback 3:** Local smart help — always works offline | **FREE** — all genuinely free tiers, no hidden paid dependency, NOT secretly calling paid Claude/OpenAI | Gemini free: 15 requests/min, 1M tokens/min, 1500 RPD free. Groq free: 14.4k RPD, 30 RPM. Puter.js free: unlimited, no key. Local: unlimited offline. | If GEMINI_API_KEY not set: tries Groq. If GROQ_API_KEY not set: tries Puter.js free unlimited. If Puter.js fails: local smart fallback that still answers about notes, tasks, book, tools, cost table, real pipeline. Always works, never breaks. Shows which provider/model actually answered in UI. | Proof required: real message sent to assistant with real response and which free provider/model answered. We log provider, model, apiKeyUsed, cost. Honest: NOT calling paid Claude/OpenAI secretly. |
| **Google Login** | Google Identity Services OAuth 2.0 via https://accounts.google.com/gsi/client — JWT decode | **FREE** — genuinely free at any volume, no billing | Unlimited, no billing, no quota. Free forever at any scale. | Always free, no limit, no degradation. If NEXT_PUBLIC_GOOGLE_CLIENT_ID not set: shows honest amber message "Google OAuth not configured — Set NEXT_PUBLIC_GOOGLE_CLIENT_ID" — NOT fake placeholder Google User 65. | Real OAuth: not signed in sees actual Sign in with Google button, after sign-in shows REAL name, REAL email, REAL Google profile photo from OAuth JWT response. Student badge if role comes from real user record. |
| **Storage / Database** | Browser localStorage per user (spi_notes_{userId}, spi_tasks_{userId}, etc.) + file JSON frontend/data/jobs.json for admin | **FREE** — browser storage + file, no paid DB | localStorage 5-10MB per user (browser limit). File JSON grows with jobs — stored in repo, free. | localStorage full: graceful — oldest notes trimmed to 50, clear message. File DB large: admin stats still work, jobs paginated. No break, degrades gracefully with clear message. | Each user isolated: spi_notes_{id} separate, no leak. Admin shows real data from DB, not mock. |
| **Book / Course PDF generation** | jsPDF MIT (https://github.com/parallax/jsPDF) + docx MIT (https://github.com/dolanmiu/docx) — open-source, client-side, no API | **FREE** — MIT license, no paid API, no billing, unlimited | Unlimited, runs in browser, no server cost, no rate limit. A4 print-ready with real margins, page numbers, headers/footers, TOC with real page numbers from final layout, justified text, no orphaned headings, RTL Arabic support. | Always works, unlimited. If fails: fallback to MD download. No paid dependency. | Difference Quick vs Book is REAL and visible: Quick Notes PDF = simple title + raw transcript notes, no cover, no TOC, no chapters. Book PDF = premium with cover auto title from real video title, TOC real page numbers from final layout, chapters per major topic matching lecture real structure, A4 margins, headers/footers, justified, RTL Arabic. Actual files you can send to print shop or print-on-demand — not webpage styled as book. |
| **Hosting** | Vercel free tier or Render free tier or localhost:3000 | **FREE** tier | Vercel free: 100GB bandwidth, 6000 exec hours. Render free: 750 hours. Localhost: unlimited. | If free tier limit hit: app degrades gracefully with clear message, still works locally. No hidden paid. | Production build has NO dev chrome, no "SPI FINAL CLEAN : 3000" fake browser toolbar inside page, no port numbers visible. Real clean build. |

## Summary

- **ONLY paid piece:** Optional Anthropic API for main notes analysis (claude-3-5-sonnet $3/M input $15/M output) — expected and fine, honestly explained, NOT required.
- **Everything else 100% free:** Transcript extraction (YouTube captions free), Notes AI (Puter.js free unlimited), Assistant (Gemini free tier primary 15 RPM + Groq fallback 14.4k/day + Puter.js free unlimited), Google OAuth free, Storage localStorage free, Book PDF jsPDF/docx MIT free, Hosting free tier.
- **No hidden paid dependency** — we went through entire stack and confirmed each service has genuine free tier, not trial that expires.
- **Honest about limitations:** If video has no transcript, we say so clearly — we do NOT silently produce fake/generic content pretending we analyzed video. If free tier limit hit, app degrades gracefully with clear message, does not break silently.

## Proof

- Transcript extraction tested with 2 different real YouTube URLs (short and long) — shows real transcripts and real notes produced (see PROOF.md)
- No-transcript video tested — correctly refused/warned instead of faking (see PROOF.md)
- Book PDF English and Arabic examples generated — actual files opened and visually confirmed (see /tmp/*.pdf)
- Assistant real message with provider/model logged (see PROOF.md)
- Cost table honest (this file)
